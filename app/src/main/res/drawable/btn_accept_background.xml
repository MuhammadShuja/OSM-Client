<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true" >
        <shape android:shape="rectangle" >

            <!-- View background color -->
            <solid
                android:color="@color/cornflower_blue_two_dark" >
            </solid>

            <stroke
                android:width="1dp"
                android:color="@color/cornflower_blue_two_dark" />

            <!-- The radius makes the corners rounded -->
            <corners
                android:radius="5dp">
            </corners>
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle" >

            <!-- View background color -->
            <solid
                android:color="@color/cornflower_blue_two" >
            </solid>

            <stroke
                android:width="1dp"
                android:color="@color/cornflower_blue_two_dark" />

            <!-- The radius makes the corners rounded -->
            <corners
                android:radius="5dp">
            </corners>
        </shape>
    </item>
</selector>