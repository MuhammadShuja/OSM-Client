<?xml version="1.0" encoding="utf-8"?>

<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="8dp">

    <com.osm.widgets.RoundCornerImageView
        android:id="@+id/ivMergerIn"
        android:layout_width="208dp"
        android:layout_height="124dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_merger_incoming"
        android:maxWidth="200dp"
        android:padding="8dp"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_editor_absoluteX="0dp" />

    <TextView
        android:id="@+id/tvTimeIn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="11:40"
        android:textSize="10sp"
        app:layout_constraintStart_toStartOf="@+id/ivMergerIn"
        app:layout_constraintTop_toBottomOf="@+id/ivMergerIn"
        tools:layout_editor_absoluteX="0dp" />

</android.support.constraint.ConstraintLayout>